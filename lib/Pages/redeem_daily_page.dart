import 'package:flutter/material.dart';
    
class RedeemDailyPage extends StatelessWidget {

  const RedeemDailyPage({ Key? key }) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('daily'),
      ),
      body: Container(),
    );
  }
}